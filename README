srv - simple synchronyous server whch creates 1 thread for each new connection

srv_async - asynchronyous server which handles multiple connection in 1 thread using coroutines

Compile both servers:

make all
